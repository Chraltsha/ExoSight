def propagate_satellite(
    satellite,
    observer,
    observation_time,
):
    """
    Compute the apparent position of one satellite.
    """



def propagate_satellites(
    satellites,
    observer,
    observation_time,
    exposure_duration,
):
    """
    Propagate all satellites during the observation.
    """


